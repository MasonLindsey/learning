"""
Password Strength Checker - STARTER TEMPLATE
You will fill in the missing parts!

Instructions: Replace the ??? with actual code
"""

def check_password_strength(password):
    """
    Check how strong a password is
    
    TODO: You need to:
    1. Create a variable called 'score' and set it to 0
    2. Create a variable called 'feedback' and set it to an empty list []
    3. Add checks below (see instructions)
    """
    
    # YOUR CODE HERE
    score = 0
    feedback = []
    
    
    # CHECK 1: Is the password long enough?
    # TODO: Check if len(password) >= 8
    # If YES: add 1 to score, print a checkmark
    # If NO: add this to feedback: "❌ Password should be at least 8 characters"
    
    if len(password) >= 8:
        score = score + 1
    else:
        feedback.append("❌ Password should be at least 8 characters")
    
    
    # CHECK 2: Does it have uppercase letters?
    # TODO: Check if password contains any A-Z
    # Hint: You can use password.isupper() or check for 'A' in password
    # If YES: add 1 to score
    # If NO: add to feedback: "❌ Add at least one uppercase letter (A-Z)"
    
    
    
    # CHECK 3: Does it have lowercase letters?
    # TODO: Similar to CHECK 2, but for lowercase (a-z)
    # Hint: Use password.islower()
    
    
    
    # CHECK 4: Does it have numbers?
    # TODO: Check if password contains any 0-9
    # Hint: You can use password.isdigit() or check for '0' in password
    
    
    
    # Return the results
    return {
        "password": password,
        "score": score,
        "feedback": feedback
    }


# MAIN PROGRAM - This part is done for you
if __name__ == "__main__":
    print("🔐 PASSWORD STRENGTH CHECKER")
    
    # Test passwords
    test_passwords = [
        "abc",
        "password",
        "Pass123",
        "MySecure@Pass"
    ]
    
    for pwd in test_passwords:
        result = check_password_strength(pwd)
        print(f"\nPassword: {'*' * len(pwd)}")
        print(f"Score: {result['score']}")
        print(f"Feedback: {result['feedback']}")
